[project]
name = "quant-harness"
version = "0.1.0"
description = "quant_harness — symbol-agnostic research and execution harness"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipython>=9.13.0",
]
